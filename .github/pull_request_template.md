## Summary

<!-- What changed and why (1–3 sentences). Link booklet § if behavior/docs changed. -->

## Test plan

- [ ] `mvn test`
- [ ] Runtime smoke (if OAuth/HTTP/security changed): `./scripts/quickstart-smoke.sh` and/or CI `build` job
- [ ] Docs updated (`docs/booklet.md`, `CHANGELOG.md`) when surface changed

## Notes

<!-- Breaking changes, env vars, honest limits (no Event Bus / MCP PRM claims unless shipped). -->
