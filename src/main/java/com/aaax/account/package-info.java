/**
 * Account domain: JPA entity, repository, and <b>application use cases</b>
 * ({@code application} subpackage) for register / password / MFA / admin / federation.
 *
 * <p>Start reading: {@link com.aaax.account.application.RegisterAccountUseCase}.
 */
package com.aaax.account;
