/**
 * Optional HTTP clients for external mesh services (tenant, idv, …).
 * Quinsic-only clients (GrandPay, Onboarding, Profile) were removed for OSS.
 * Default base URLs are disabled placeholders — set env when integrating.
 */
package com.aaax.server.ext.api.client;
