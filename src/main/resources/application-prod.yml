# Production-ish defaults: no demo toys.
aaax:
  demo:
    seed-client: false
    seed-account: false
  otp:
    channel: mail

spring:
  jpa:
    hibernate:
      ddl-auto: validate
  sql:
    init:
      mode: never
